{
  "name": "@freshair129/skill-creator",
  "version": "0.1.0",
  "private": true,
  "description": "Skill creation tools for Gemini CLI and other agents",
  "type": "module",
  "scripts": {
    "typecheck": "echo \"Python package: skipping TS typecheck\" && exit 0",
    "build": "echo \"Python package: no build step\" && exit 0",
    "test": "echo \"Python package: no test step\" && exit 0"
  }
}
