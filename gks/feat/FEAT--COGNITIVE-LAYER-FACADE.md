---
id: FEAT--COGNITIVE-LAYER-FACADE
phase: 2
type: feat
status: stable
vault_id: default
tier: process
source_type: axiomatic
title: createCognitiveLayer — one-line memoryOS entry for any cognitive-layer agent
tags:
  - msp
  - cognitive-layer
  - facade
  - memoryos
  - user-facing
crosslinks: {"implements":["CONCEPT--COGNITIVE-LAYER-FACADE"],"references":["CONCEPT--AGENT-AGNOSTIC","ADR--MEMORY-STORE-GRAPH-WIRING","ADR--GEMINI-AS-SLM-PROVIDER","ADR--DEFAULT-SLM-OLLAMA-QWEN-CODER"]}
linked_symbols:
  - {"file":"packages/msp/src/cognitive/index.ts"}
  - {"file":"packages/msp/src/cognitive/types.ts"}
  - {"file":"packages/msp/src/cognitive/fts.ts"}
  - {"file":"packages/msp/src/cognitive/scale-gate.ts"}
  - {"file":"packages/msp/src/cognitive/ssot.ts"}
  - {"file":"packages/msp/src/cognitive/audit-only.ts"}
  - {"file":"packages/msp/src/cognitive/compose.ts"}
created_at: 2026-05-12T22:50:00.000+07:00
aliases:
  - FEAT
  - implementation_flow
  - Feature spec
---

# FEAT — createCognitiveLayer

## User-facing behaviour

```ts
import { createCognitiveLayer } from 'msp'

const layer = await createCognitiveLayer({
  root: process.cwd(),
  slm: { tier: 'T1' },              // default — local Ollama + qwen2.5-coder
})

// 4-layer hybrid recall (FRAMEWORK_MASTER_SPEC §13)
const hits = await layer.recall('how did we decide rate limiting?')

// Episodic results are stamped audit_only (§7.5)
hits.hits.filter((h) => h.audit_only)

// Write path
await layer.remember('Cortex handles planning.', { tags: ['cortex'] })

// Codegen with scale-level gate (§7.7.2)
await layer.runTask('./.brain/tasks/FEAT--X/T1.task.yaml', { scale: 'L2' })

// SSOT conflict resolver (§14.1)
const winner = layer.resolveSSOT([
  { id: 'PROTO--X', type: 'proto', source: 'atom' },
  { id: 'ADR--X',   type: 'adr',   source: 'atom' },
])
// → { id: 'PROTO--X', ... }

// Hotfix escape hatch (§6.4)
await layer.hotfix.open({ sha: 'abc1234', reason: 'urgent rollback' })

// Spawn the existing 19-tool MCP surface for the consuming agent
const server = layer.mcpServer()
```

## §-references honoured

| Spec § | Mechanism |
|---|---|
| §5.1 / §5.4 | `recall` consults atomic exact-id first; fan-out to FTS / vector / graph follows. |
| §7.5 | Episodic hits stamped `audit_only: true`. |
| §7.7.2 | `enforceScaleGate()` in `runTask`. |
| §8.1 / §17.3 | `slm.tier` knob (T1/T2/T3) → provider routing. |
| §9.6 | `composeWithMarker()` + `buildAutoGeneratedMarker()`. |
| §13 | 4-layer hybrid pipeline (atomic → FTS → vector → graph + RRF blend). |
| §14.1 | `resolveSSOT()` authority hierarchy. |
| §6.4 | `hotfix.{open,list,close,check}` re-exports. |
| §17.1 | Path encoding flows through existing `packages/msp/src/projects/resolve.ts`. |

## Acceptance criteria

- [ ] `createCognitiveLayer({ root })` resolves without `slm` / `graphBackend` / `obsidian` opts.
- [ ] `layer.remember()` returns `{ id: string }`.
- [ ] `layer.recall()` returns a `CognitiveRecallResult` whose hits carry `audit_only: true` for episodic source.
- [ ] `layer.runTask()` with `scale: 'L2'` and a blueprint missing CONCEPT / ADR / FEAT throws `ScaleLevelGateError`.
- [ ] `layer.mcpServer()` returns a `McpServer` instance.
- [ ] Default `slm.tier: 'T1'` resolves to provider `'ollama'` (validated via `tier-routing.test.ts`).

## Out of scope

- New MCP tools (none added).
- Atom-write paths (still through `msp_candidate` + PR).
- Full §8.4 slot/layout grammar for the deterministic composer.

## Connections
- [[CONCEPT--COGNITIVE-LAYER-FACADE]]
- [[CONCEPT--AGENT-AGNOSTIC]]
- [[ADR--MEMORY-STORE-GRAPH-WIRING]]
- [[ADR--GEMINI-AS-SLM-PROVIDER]]
- [[ADR--DEFAULT-SLM-OLLAMA-QWEN-CODER]]

