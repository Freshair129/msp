{
  "name": "@freshair129/gks-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GKS MCP server — exposes GKS tools to AI agents via Model Context Protocol",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "echo 'No tests for skeleton app' && exit 0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "~5.4.0",
    "@types/node": "^20.0.0"
  }
}
