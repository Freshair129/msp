{
  "name": "@freshair129/gks-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GKS terminal UI — interactive knowledge graph explorer in the terminal",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for skeleton app' && exit 0"
  },
  "dependencies": {
    "ink": "^5.0.0",
    "react": "^18.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "~5.4.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0"
  }
}
