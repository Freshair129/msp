## Task: Write TS adapter for P3.2 Genesis Block native backend

You are operating on branch `claude/p3-2-storage-mvp-hybrid` in the cognitive_system monorepo (Windows host). Claude Code is also active on this same branch but is working on `packages/gks/native/genesis-block/` (Rust crate). Your scope is **strictly inside `packages/gks/src/memory/`** — do not touch the Rust crate or any other directory.

## Context

- BLUEPRINT--GENESIS-GRAPH-INTEGRATION P3.2 requires a TypeScript adapter that delegates `GraphBackend` calls to the new native Rust crate while preserving a fallback to the existing pure-TS implementation.
- The native crate (already implemented) exposes a `GenesisDatabase` class via napi-rs. Its typings are at `packages/gks/native/genesis-block/index.d.ts` — read this file first to confirm the exact API surface.
- The current pure-TS implementation is at `packages/gks/src/memory/graph/genesis-graph.ts` and exports `GenesisGraphBackend` (a class) + `createGenesisGraphBackend` (a factory). The `GraphBackend` interface itself lives in `packages/gks/src/memory/graph.ts`.
- ADR--GENESIS-BLOCK-STORAGE-LAYOUT (merged as PR #152, on origin/main) chose JSONL event log as the on-disk format. The Rust crate and the pure-TS impl both write the SAME `<path>/genesis-graph.jsonl` + `<path>/manifest.json` files — verify this before assuming the adapter logic.

## What to deliver

1. **Update `packages/gks/src/memory/graph/genesis-graph.ts`** so that `createGenesisGraphBackend({ path })`:
   - Tries to `require('@freshair129/gks-genesis-block-native')` (the native module).
   - If load succeeds: returns a new adapter class instance that wraps `GenesisDatabase.open({ path })` and implements every `GraphBackend` method (`load`, `addNode`, `addEdge`, `retractEdge`, `query`, `neighbors`) by delegating to the native methods. Match the existing function signatures exactly. The native class returns typed values (NodeOutput, EdgeOutput, NeighborOutput) — convert these into `GraphNode` / `GraphEdge` / `NeighborResult` shapes from `../graph.ts`.
   - If load fails (module not found, .node not built for this triple, etc.): silently fall back to the existing pure-TS `GenesisGraphBackend` — do NOT throw. Log a single info-level message via `createLogger('graph:genesis-graph')`.
   - The existing `GenesisGraphBackend` class and the Cypher v0 fallback path stay intact — keep them as the fallback target. The adapter is *additive*.

2. **Wire the native package into `packages/gks/package.json`** as an `optionalDependencies` entry: `"@freshair129/gks-genesis-block-native": "workspace:*"`. If the entry already exists or the file is otherwise dirty, do NOT modify other fields.

3. **Update `packages/gks/native/genesis-block/package.json`** ONLY if its `"name"` field is not `"@freshair129/gks-genesis-block-native"` — bring it in line. If it already matches, do nothing to this file. (Claude is working in this crate's Rust files; you may read but should not touch anything in `src/`, `Cargo.toml`, `index.js`, or `index.d.ts` there.)

4. **Verify with `npx tsc -p packages/gks/tsconfig.json --noEmit`** that nothing broke. If it errors, fix.

## Constraints

- Do NOT touch any file outside `packages/gks/src/memory/` and `packages/gks/package.json` and `packages/gks/native/genesis-block/package.json`.
- Do NOT git commit. Claude will review the diff and commit on this branch.
- Do NOT git push.
- Do NOT modify any `.md` file in `gks/` (atom vault). The vault has a known recurring auto-edit problem from another agent — leave atoms alone.
- Do NOT create new files outside the paths listed above.
- Windows host: use forward slashes in TS paths, no shell-specific syntax.
- The pure-TS `GenesisGraphBackend` already in `genesis-graph.ts` is the BEHAVIOURAL reference. The adapter wrapping native must return the same shapes for the same inputs — tests parametrised over both backends will run later.

## When done

Print a short summary to stdout describing:
- Files modified (paths)
- The shape of the load-detection logic
- Any compromises or notes for Claude's review
- Whether `tsc --noEmit` passed

Do not start any new task after that — exit so Claude can review and commit.
